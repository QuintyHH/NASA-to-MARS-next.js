export default {
  launchButton: 'Launch Mission'.toUpperCase(),
  disabledText: 'Mission file not loaded yet'.toUpperCase(),
  resetMission: 'Reset Mission'.toUpperCase(),
  notification: {
    success: {
      type: 'success',
      message: 'Mission accomplished!'.toUpperCase(),
    },
    fail: {
      type: 'fail',
      message: 'Something went wrong...',
    },
  },
}
